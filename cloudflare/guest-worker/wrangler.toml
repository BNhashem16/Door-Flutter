name = "door-gate"
main = "src/index.js"
compatibility_date = "2024-09-23"

# DB_SECRET is NOT set here — it is a secret, injected via:
#   wrangler secret put DB_SECRET
# (paste the Firebase RTDB legacy database secret when prompted)
